{#
  IBM MQ QueueManager custom resource template for SingleInstance mode.
  Provides the base CR for non-NativeHA queue managers while keeping the same
  naming, PKI, MQSC, INI, storage, and monitoring conventions.
  qmgr onboarding facts: qmgr_name, namespace, current_region,
  qmgr_name_upper, qmgr_size, mqsc_core_files, ini_files.
  node selector, metrics, and optional script settings.
  base/qmgr.yaml for a single queue manager.
#}
{% set _webui_enabled = mq_webui_enabled | default(enable_webui | default(web_enabled | default(false))) | bool %}
{% set _operator_metrics_enabled = mq_operator_metrics_enabled | default(enable_metrics | default(false)) | bool %}
---
# sgl-qmgr.yaml - SingleInstance QueueManager CR
# Populated and deployed by Ansible automation (nativeha_templates)
# instance_type: single
apiVersion: mq.ibm.com/v1beta1
kind: QueueManager
metadata:
  name: {{ qmgr_name }}
  namespace: {{ namespace }}
  labels:
    mq-qmgr: {{ qmgr_name }}
    app.kubernetes.io/name: {{ qmgr_name }}
    app.kubernetes.io/instance: {{ qmgr_name }}-{{ current_region }}
    app.kubernetes.io/managed-by: ansible-automation
    mq.ibm.com/environment: {{ mq_environment | upper }}
    mq.ibm.com/instance-type: single
    mq.ibm.com/region: {{ current_region }}
spec:
  annotations:
    {{ restart_annotation_prefix }}/restart-at: "initial"
    {{ restart_annotation_prefix }}/restart-reason: "onboarded"
    {{ restart_annotation_prefix }}/restart-requested-by: "ansible-automation"
  version: PLACEHOLDER_MQ_VERSION
  license:
    accept: true
    license: PLACEHOLDER_LICENSE_ID
    cluster1: {{ license_use }}
  imagePullSecrets:
    - name: ibm-entitlement-key

  web:
    enabled: {{ _webui_enabled | ternary('true', 'false') }}
{% if _webui_enabled %}
    console:
      authentication:
        provider: {{ web_authentication }}
      authorization:
        provider: {{ web_authorization }}
    manualConfig:
      configMap:
        name: {{ qmgr_name }}-{{ web_configmap }}
{% endif %}

  pki:
    keys:
      - name: default
        secret:
          secretName: "{{ qmgr_name }}-app-pki"
          items:
            - tls.key
            - tls.crt
            - ca.crt
    trust:
      - name: {{ mq_client_trust_source_name | default('clientcerts') }}
        secret:
          secretName: "{{ qmgr_name }}-{{ mq_client_trust_secret_suffix | default('client-certs') }}"
          items:
            - {{ mq_client_trust_default_ca_item | default('example.lab-ca.crt') }}

  template:
    pod:
      volumes:
{#
Section:
  Optional script ConfigMap mount. Generated only when script files exist and
  enable_scripts_configmap is true.
#}
{% if enable_scripts_configmap | default(true) | bool and script_files | default([]) | length > 0 %}
        - name: scripts
          configMap:
            name: "{{ qmgr_name }}-utility-scripts"
            defaultMode: 0755
{% endif %}
        - name: mqsc-config
          projected:
            defaultMode: 0755
            sources:
              - configMap:
                  name: {{ qmgr_name }}-mqsc
{% if app_mqsc_mount_in_pod | default(true) | bool and app_mqsc_groups | default([]) | length > 0 %}
        - name: mqsc-app-config
          projected:
            defaultMode: 0444
            sources:
{% for group in app_mqsc_groups | default([]) %}
              - configMap:
                  name: {{ qmgr_name }}-mqsc-{{ group.suffix }}
{% endfor %}
{% endif %}
      containers:
        - name: qmgr
          volumeMounts:
{% if enable_scripts_configmap | default(true) | bool and script_files | default([]) | length > 0 %}
            - name: scripts
              mountPath: /scripts
              readOnly: true
{% endif %}
            - name: mqsc-config
              mountPath: /mq-config
              readOnly: true
{% if app_mqsc_mount_in_pod | default(true) | bool and app_mqsc_groups | default([]) | length > 0 %}
            - name: mqsc-app-config
              mountPath: /mq-app-config
              readOnly: true
{% endif %}
          resources:
            requests:
              cpu: "{{ qmgr_size.request_cpu }}"
              memory: "{{ qmgr_size.request_memory }}"
            limits:
              cpu: "{{ qmgr_size.limits_cpu }}"
              memory: "{{ qmgr_size.limits_memory }}"
      nodeSelector:
        {{ node_selector_key }}: "{{ node_selector_value }}"
      tolerations:
        - key: {{ node_selector_key }}
          operator: Equal
          value: "{{ node_selector_value }}"
          effect: NoSchedule
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: topology.kubernetes.io/zone
              labelSelector:
                matchLabels:
                  mq-qmgr: {{ qmgr_name }}
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                topologyKey: topology.kubernetes.io/hostname
                labelSelector:
                  matchLabels:
                    mq-qmgr: {{ qmgr_name }}

  queueManager:
    name: {{ qmgr_name_upper }}
    image: PLACEHOLDER_MQ_IMAGE
    mqsc:
      - configMap:
          name: {{ qmgr_name }}-mqsc
          items:
{#
Section:
  Core MQSC file references discovered from nativeha_templates/base/mqsc.
#}
{% for file in mqsc_core_files %}
            - {{ file }}
{% endfor %}
    ini:
      - configMap:
          name: "{{ qmgr_name }}-ini"
          items:
{#
Section:
  INI file references discovered from nativeha_templates/base/ini.
#}
{% for file in ini_files %}
            - {{ file }}
{% endfor %}
    availability:
      type: SingleInstance
    metrics:
      enabled: {{ _operator_metrics_enabled | ternary('true', 'false') }}
{% if _operator_metrics_enabled %}
      tls:
        provider: openshift
{% endif %}
    readinessProbe:
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 3
      successThreshold: 1
    recoveryLogs:
      logFilePages: {{ qmgr_size.log_file_pages | default(32768) }}
    storage:
      allowVolumeExpansion: {{ (allow_volume_expansion | default(true) | bool) | ternary('true', 'false') }}
      queueManager:
        type: persistent-claim
        class: {{ storage_class_name }}
        size: "{{ qmgr_size.qmgr_storage }}"
      recoveryLogs:
        type: persistent-claim
        enabled: true
        class: {{ storage_class_name }}
        size: "{{ qmgr_size.log_storage }}"
