---
# vaultstaticsecret_app_pki.yaml — VSO VaultStaticSecret for mTLS app PKI
# Syncs from Vault KV v2: {{ vault_mount }}/data/ibm-mq/{{ mq_environment | upper }}/{{ qmgr_name | lower }}/app-pki
# Creates K8s secret: {{ qmgr_name }}-app-pki (tls.key, tls.crt, ca.crt)
# Rolling update: Pod restart triggered on cert change via rolloutRestartTargets
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: {{ qmgr_name }}-app-pki
  namespace: {{ namespace }}
  labels:
    app.kubernetes.io/name: {{ qmgr_name }}
    app.kubernetes.io/managed-by: ansible-automation
    mq.ibm.com/cert-type: app-pki
spec:
  vaultAuthRef: {{ vso_vault_auth_name }}
{% if vault_namespace %}
  namespace: {{ vault_namespace }}
{% endif %}
  mount: {{ vault_mount }}
  type: kv-v2
  path: {{ app_prefix }}/ibm-mq/{{ mq_environment | upper }}/{{ namespace | upper }}/{{ qmgr_name_upper }}/app-pki
  refreshAfter: {{ vso_refresh_after }}
  destination:
    name: {{ qmgr_name }}-app-pki
    create: true
    type: kubernetes.io/tls
  rolloutRestartTargets:
    - kind: StatefulSet
      name: {{ qmgr_name }}
