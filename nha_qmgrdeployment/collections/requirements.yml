---
# ============================================================
# Collection Requirements — MQ Queue Manager Onboarding
# ============================================================
# AAP Project sync looks for collections/requirements.yml
# when the Project SCM points to this subfolder.
# Install via project sync or:
#   ansible-galaxy collection install -r collections/requirements.yml
#
# Required for:
#   - human_to_bytes / human_readable filters (storage sizing)
# ============================================================

collections:
  - name: community.general
    version: ">=8.0.0"
    source: https://galaxy.ansible.com
